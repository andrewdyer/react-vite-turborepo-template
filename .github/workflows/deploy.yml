name: Deploy

on:
  repository_dispatch:
    types:
      - deploy

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    # To require manual approval before a deploy is executed, uncomment
    # the line below. The environment must first be configured under
    # Settings → Environments with a protection rule and required reviewers.
    # environment: production  ← uncomment this line
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Deploying ${{ github.event.client_payload.app }}@${{ github.event.client_payload.version }}"
          # Add deployment steps here
